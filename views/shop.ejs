<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cửa hàng</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav style="background-color: #2c5d45; padding: 10px;">
        <a href="/" style="color: white; margin-right: 20px;">Trang chủ</a>
        <a href="/shop" style="color: white; margin-right: 20px;">Sản phẩm</a>
        <a href="/add-product" style="color: white;">Thêm sản phẩm</a>
    </nav>

    <h1 style="text-align: center;">Danh sách sản phẩm</h1>

    <div style="display: flex; justify-content: center; flex-wrap: wrap;">
        <% products.forEach(product => { %>
            <div style="border: 1px solid #ddd; padding: 20px; margin: 10px; width: 200px; text-align: center;">
                <img src="<%= product.image %>" alt="<%= product.name %>" style="width: 100px; height: 100px;">
                <h3><%= product.name %></h3>
                <p>Giá: <%= product.price %> VNĐ</p>
            </div>
        <% }); %>
    </div>
</body>
</html>
